// requires routes, config, run they implicit requiring the app
//require引入路径。
require([
	'app'
], function (app) {
  'use strict';
  //app.init();
  // Here you have to set your app name to bootstrap it manually
 // angularAMD.bootstrap(document, ['app']);
});
